node_modules/
*.log
.env
.env.local
*.key

# rust / mopro build output
prover/target/
prover/build/

# compiled native libs + frameworks — regenerated by `npm run setup`
*.a
*.xcframework/
prover/MoproIOSBindings/
prover/MoproiOSBindings/

# proving keys + witness generators — fetched from upstream by `npm run setup`
*.zkey
*.wasm
prover/react-native/android/app/src/main/assets/custom/

# ios
prover/react-native/ios/Pods/
prover/react-native/ios/build/
prover/react-native/build/
prover/react-native/ios/dd/
*.so

# upstream circuits, fetched by setup.sh
vendor/
